<h1><%= t('.gist_list') %></h1>

<table class="table table-striped">
  <tr class="table-primary">
    <th><%= t('.question') %></th>
    <th><%= t('.gist_url') %></th>
    <th><%= t('.user') %></th>
  </tr>
  <%= render @gists %>
</table>

<%= link_to t('.back'), :root %>
