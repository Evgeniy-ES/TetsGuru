<tr>
  <td class="title"><%= question.body %></td>
  <td class="test_id"><%= question.test_id %></td>
  <td><%= link_to t('.show'), admin_question_path(question) %></td>
  <td class="functions"><%= link_to t('.edit'), edit_admin_question_path(question) %></td>
  <td class="functions"><%= link_to t('.delete'), admin_question_path(question), method: :delete, data: { confirm: "Are you sure?" } %></td>
</tr>
