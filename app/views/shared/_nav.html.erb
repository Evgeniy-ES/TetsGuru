<nav class="navbar navbar-light bg-light">
  <h2 class="header">
   <% if current_user && current_user.admin? %>
     <%= link_to 'Admin TestGuru', admin_tests_path, class: 'navbar-brand' %>
   <% else %>
     <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
   <% end %>
 </h2>

  <div class="card border-primary">
    <div class="card-body">
      <span class="navbar-text">
        <% if user_signed_in? %>
            <%= t('.welcome') %>, <b><%= current_user.first_name %></b> <%= t('.guru') %> |
            <%= button_to t('.logout'), destroy_user_session_path, method: :delete, class: 'btn btn-info' %>
        <% else %>
          <%= link_to "Log in", new_session_path(resource_name) %> |
          <%= link_to "Sign up", new_registration_path(resource_name) %>
        <% end %>
      </<span>
    </div>
  </div>
</nav>
